import { Menu } from '@/shared/ui';
import { styled } from 'styled-components';

export const StyledMenu = styled(Menu)`
  display: flex;
  align-items: center;
  list-style: none;
  column-gap: 32px;
  margin: 0;
  padding: 0;
`;
